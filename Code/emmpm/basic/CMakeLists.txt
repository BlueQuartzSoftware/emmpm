project(basic)

set(basic_SRCS
 ${basic_SOURCE_DIR}/em.c
)

# add_executable(basic ${basic_SRCS})
# target_link_libraries(basic emmpmCommon)
# ToolInstallationSupport(basic ${EXE_DEBUG_EXTENSION} ${basic_BINARY_DIR})
